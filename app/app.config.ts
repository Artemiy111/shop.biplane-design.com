export default defineAppConfig({
  ui: {
    colors: {
      primary: 'amber',
    },
    button: {
      slots: {
        base: 'font-bold cursor-pointer',
      },
    },
  },
  icon: {
    mode: 'svg',
  },
})
